import React from 'react';
import Navbar from '../Navbar/Navbar';
import SearchArea from '../SearchArea/SearchArea';

const Home = () => {
    return (
        <React.Fragment>
            <Navbar/>
            <SearchArea/>
        </React.Fragment>
    );
};

export default Home;